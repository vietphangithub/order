import { CustomUrlPipe } from './custom-url.pipe';

describe('CustomUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
